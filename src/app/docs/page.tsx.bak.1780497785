import Link from 'next/link';

export default function DocsIndex() {
  const sections = [
    {
      title: 'Architecture',
      description: 'How the system works end-to-end',
      href: '/docs/architecture',
    },
    {
      title: 'Signal Targets',
      description: 'LTE, 5G, DTV, FM, and future signal sources',
      href: '/docs/signals',
    },
    {
      title: 'Tokenomics',
      description: 'Reward structure and token utility',
      href: '/docs/tokenomics',
    },
    {
      title: 'FAQ',
      description: 'Common questions answered',
      href: '/docs/faq',
    },
  ];

  return (
    <div className="not-prose">
      <h1 className="text-4xl font-bold text-slate-100 mb-4">
        Azimuth Documentation
      </h1>
      <p className="text-lg text-slate-400 mb-12">
        Welcome to the Azimuth documentation. Azimuth is a decentralized network
        of passive SDR receivers building a global positioning and timing layer
        from signals of opportunity. These docs cover the architecture, signal
        types, tokenomics, and frequently asked questions.
      </p>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        {sections.map((section) => (
          <Link
            key={section.href}
            href={section.href}
            className="group bg-surface border border-border rounded-xl p-6 hover:-translate-y-1 transition-all hover:border-amber-500/50"
          >
            <h3 className="text-xl font-semibold text-slate-100 group-hover:text-amber-500 transition-colors mb-2">
              {section.title}
            </h3>
            <p className="text-slate-400">{section.description}</p>
          </Link>
        ))}
      </div>
    </div>
  );
}
