import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
  description: "Monitor your node status and rewards on the Azimuth network.",
};

export default function DashboardLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <div className="bg-navy text-slate-100 min-h-screen flex">
      {/* Sidebar */}
      <aside className="hidden md:block w-64 bg-surface border-r border-border">
        <nav className="flex flex-col p-6 space-y-2">
          <a
            href="/dashboard"
            className="flex items-center gap-3 px-4 py-2 rounded-lg bg-surface-alt text-amber-500 font-medium transition-all"
          >
            <span className="text-lg">⊞</span>
            <span>Overview</span>
          </a>
          <a
            href="/dashboard"
            className="flex items-center gap-3 px-4 py-2 rounded-lg text-slate-400 hover:text-slate-100 hover:bg-surface-alt/50 transition-all"
          >
            <span className="text-lg">⚙</span>
            <span>My Node</span>
          </a>
          <a
            href="/dashboard"
            className="flex items-center gap-3 px-4 py-2 rounded-lg text-slate-400 hover:text-slate-100 hover:bg-surface-alt/50 transition-all"
          >
            <span className="text-lg">💰</span>
            <span>Rewards</span>
          </a>
          <a
            href="/dashboard"
            className="flex items-center gap-3 px-4 py-2 rounded-lg text-slate-400 hover:text-slate-100 hover:bg-surface-alt/50 transition-all"
          >
            <span className="text-lg">⚙️</span>
            <span>Settings</span>
          </a>
        </nav>
      </aside>

      {/* Main Content */}
      <main className="flex-1 p-6 md:p-8">{children}</main>
    </div>
  );
}
